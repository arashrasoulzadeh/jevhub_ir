/*
 * تنظیمات سراسری سایت.
 * برای به‌روزرسانی تاریخ مستندات فقط همین فایل را ویرایش کنید؛
 * تاریخ‌ها به‌صورت خودکار به تقویم شمسی و ارقام فارسی نمایش داده می‌شوند.
 *
 * توجه: شناسه‌ی Google Analytics این‌جا نیست. Google صریحاً می‌خواهد آن تگ
 * literal در HTML بلافاصله بعد از <head> باشد (ابزار تشخیصش جاوااسکریپت
 * اجرا نمی‌کند)، پس در خودِ صفحات است — با مقدار از .env (کلید
 * GA_MEASUREMENT_ID)، تولیدشده توسط .build-scripts/rebuild_docs.py و
 * gen_docs_index.py.
 */
window.JEV_SITE = {
  // آخرین به‌روزرسانی ترجمه‌ی فارسی مستندات (میلادی، YYYY-MM-DD)
  docsUpdated: "2026-09-23",
  // تاریخ آخرین بازبینی مستندات رسمی انگلیسی که این ترجمه بر اساس آن است
  sourceReviewed: "2026-09-17",
  // نسخه‌ی فعلی مدل
  model: "jev-1.13.0",
  alias: "jev-latest",
  sourceUrl: "https://docs.typesafe.ai",
  // آخرین باری که جدول «جایگزین‌ها» (مدل‌های مشابه) بازبینی شد — این بازار
  // به‌سرعت عوض می‌شود، این تاریخ را جدا از docsUpdated نگه می‌داریم
  similarModelsUpdated: "2026-09-24",
  // کلید عمومی (site key) ARCaptcha (arcaptcha.ir) برای کپچای Playground —
  // نه Cloudflare Turnstile، چون این پروژه روی ArvanCloud میزبانی می‌شود.
  // این مقدار محرمانه نیست، برخلاف secret key که فقط در
  // docker-compose.yml/.env سمت سرویس gate قرار می‌گیرد و هرگز این‌جا
  // نمی‌آید (باید همان مقدار SITE key را در هر دو جا دستی هم‌سان نگه
  // دارید). اگر خالی بماند، کپچا غیرفعال است و به‌جایش فقط پیام «کمی صبر
  // کنید» بعد از محدودیت نرخ نشان داده می‌شود. از
  // https://arcaptcha.ir/dashboard بگیرید.
  arcaptchaSiteKey: "",
};
